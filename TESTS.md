# Tests

This project uses Vitest. Run `npm run test`.

Tests included:

- `tests/audit.test.ts` — tests auditing rules and edge cases.

Add more tests for API routes and Supabase integration when integrating service keys.
